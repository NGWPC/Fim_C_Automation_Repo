stages:
  - build
  - test

job_build:
  stage: build
  script:
    - echo "Building the project"

job_test:
  stage: test
  variables:
    GIT_STRATEGY: none
  environment: test
  before_script:
    - echo $SSH_KEY
    - chmod 400 $SSH_KEY
    - ssh-agent
  script:
    - echo "Deploying FIMC to TEST..."
    - mkdir -p reports
    - cp  fim_c_automation/report.html reports   
    - echo "FIMC deployed to TEST"
  artifacts: 
    when: always
    paths:
       - reports/



